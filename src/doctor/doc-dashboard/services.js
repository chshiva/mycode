import utils from '../../commons/utils';

export function callDoctorDashboardApi(callback) {
	console.log(2)
    // utils.httpRequest('doctordashboard', 'get', {}, (response) => {
    //     console.log("doctordashboard response ", response);
    //     callback(response);
    // });
}
